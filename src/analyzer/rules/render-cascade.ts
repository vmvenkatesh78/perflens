import type { ComponentPerfData, Insight, PerfLensThresholds } from '../../types';

// TODO(v0.3.0): implement — see tech design doc section 5.2
export function check(
  _name: string,
  _data: ComponentPerfData,
  _thresholds: PerfLensThresholds,
  _now?: number,
): Insight[] {
  return [];
}
